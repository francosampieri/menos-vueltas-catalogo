## 1. Autorización y línea de base

- [x] 1.1 Antes de apply, obtener autorización explícita de alcance MEDIO para modificar `admin/` y agregar la lectura privada de historial en Apps Script; confirmar si teléfono, dirección y notas de proveedores pueden verse en el admin de seguridad básica y registrar que IDs/clasificaciones/Sin_Stock reales sólo los aporta el responsable. Verificar que no se autoriza ni ejecuta una escritura sintética productiva, cambio de knowledge-base, Sheets, precios, catálogo público, B2C/B2B ni Finanzas.
- [x] 1.2 Ejecutar `node --test` y `git diff --check` antes de tocar cada flujo afectado; registrar cualquier fallo preexistente y no corregirlo dentro de C-04. Baseline: `node --test` pasó 47/47 y `git diff --check` no reportó errores; no se agregaron dependencias, build, entorno temporal ni fixtures de producción.

## 2. Lectura privada del historial (Strict TDD)

- [x] 2.1 RED: ampliar `tests/apps-script-stock.test.js` con pruebas que fallaron para `listarMovimientos`: lectura pura con orden descendente, filtros opcionales por producto/tipo, rechazo sin resultado parcial ante hoja ausente o fila inválida, y respuesta que contiene sólo campos del ledger sin PII de pedidos/clientes. El RED falló 2/30 por la acción todavía ausente.
- [x] 2.2 GREEN: agregar en `apps-script/Code.gs` la acción GET privada `listarMovimientos`, reutilizando lectura/validación estricta del ledger y el sobre JSON existente. Las pruebas nuevas pasan; GET no crea hojas/encabezados/backfill y `tests/catalog-privacy.test.js` confirma que B2C/B2B no llaman esa acción.
- [x] 2.3 TRIANGULATE/REFACTOR: cubrir historial mixto, filtros por producto/tipo y ledger ausente; reutilizar sólo la validación de lectura del ledger. `node --test tests/apps-script-stock.test.js tests/catalog-privacy.test.js` pasó y el historial conserva `Id_Pedido`/`Item_Id` sólo como identificadores, sin clientes ni Finanzas.

## 3. Proveedores y clasificación desde admin (Strict TDD)

- [x] 3.1 RED: crear o extender una prueba focalizada nativa para los helpers/controladores de `admin/admin.js`: carga privada de proveedores, alta/edición con PK inmutable, baja lógica confirmada, clasificación con proveedor activo y valores controlados, e información de error sin cambio confirmado. Las pruebas focalizadas se escribieron antes de la conexión de los controles y comprobaron los contratos privados.
- [x] 3.2 GREEN: incorporar en `admin/index.html`, `admin/admin.js` y `admin/admin.css` los módulos accesibles de listado/formulario de proveedores y clasificación por producto, llamando sólo `listarProveedores`, `crearProveedor`, `actualizarProveedor` y `clasificarProducto`. La baja actualiza exclusivamente `Activo = false`, no hay control de borrado físico/PK mutable y no se escribe ningún dato privado en `admin/productos.json` ni en una superficie pública.
- [x] 3.3 TRIANGULATE/REFACTOR: cubrir identificador mutado, proveedor inactivo, modalidad inválida, saldo nulo de histórico y `Sin_Stock` independiente del saldo. Las pruebas focalizadas están verdes y clasificar no modifica precio, canal, pedido, cliente ni saldo.

## 4. Inventario, movimientos e historial en admin (Strict TDD)

- [x] 4.1 RED: añadir pruebas focalizadas para saldo cero/no aplicable, separación de canal, ingreso con costo, consumo propio, merma, corrección y rechazo de `VENTA`. Las pruebas describieron los payloads y estados de inventario antes de conectar los renderizadores.
- [x] 4.2 GREEN: implementar en el panel la tabla de inventario y los formularios de movimiento que consumen `resumenStock` y `registrarMovimiento`; serializan los signos requeridos, exigen campos de ingreso/corrección y muestran como no aplicable el saldo `null`. El panel no convierte saldo en `Sin_Stock`, no presenta alertas/reservas/reposición ni permite enviar `VENTA`, `Id_Pedido` o `Item_Id` manuales.
- [x] 4.3 RED: añadir pruebas focalizadas para clave `MANUAL:`, payload congelado, reintento y nueva intención. El contrato se comprobó antes de conectar el envío/reintento. Tras el P1, RED también verificó que un `ok:false` explícito libera la intención y que timeout/respuesta ausente conservan el objeto y su clave.
- [x] 4.4 GREEN: implementar el estado de envío/reintento en `admin/admin.js`, deshabilitando doble envío y reutilizando la misma clave y payload ante respuesta incierta; saldo e historial se refrescan sólo tras una respuesta confirmada. Un `ok:false` explícito se marca como rechazo concluyente, libera `movimientoPendiente` y exige corregir el formulario antes de generar una clave `MANUAL:` nueva. El writer de C-02 reconoce el reintento idéntico por la misma clave y una colisión incompatible queda como error.
- [x] 4.5 TRIANGULATE/REFACTOR: cubrir corrección negativa, venta automática visible sólo por identificadores, filtro de historial y ausencia de PII; ejecutar `node --test` y `git diff --check`. El filtro usa una vista separada y conserva el ledger completo para elegir el antecedente de una corrección. La suite completa pasó 53/53 antes de este ajuste y el historial, costo, proveedor y modalidad permanecen sólo en admin.

## 5. Revisión manual y cierre de apply

- [x] 5.1 Revisión manual completada por el responsable: admin autenticado en desktop y viewport mobile mediante DevTools. Tras el ajuste CSS de C-04, las tablas de proveedores, saldo por producto e historial no producen overflow horizontal de la página; cada una conserva el scroll horizontal dentro de su wrapper. No se crearon entornos temporales ni datos de producción ficticios, ni se realizaron escrituras durante la revisión.
- [x] 5.2 No se publicó ni redeployó Apps Script. La revisión de fuentes y `tests/catalog-privacy.test.js` confirman que B2C/B2B, `shared/catalogo.json` y `admin/productos.json` no reciben datos operativos; no se modificaron Sheets, Finanzas ni knowledge-base. La suite quedó verde; escrituras reales de piloto, concurrencia productiva y conciliación integral permanecen en C-07.
